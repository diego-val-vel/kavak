services:
  api:
    build: .
    container_name: kavak_api
    working_dir: /app
    command: ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]
    volumes:
      - ./:/app
    ports:
      - "8000:8000"
    env_file:
      - .env
    stdin_open: true
    tty: true
